  <nav class="navbar navbar-default">
    <div class="container-fluid">
      <div class="navbar-header">
      </div>
    </div>
  </nav>

  <div class="container">
    <div class="row">
      {{!-- confirm brewery response objects are correct --}}
        {{brewery-name}}
        {{brewery-type}}
        {{brewery-street}}
        {{brewery-city}}
        {{brewery-state}}
        {{brewery-postal-code}}
        {{brewery-phone}}
        {{brewery-website}}
        
       
    </div>
  </div>
  